{/*'use client'

import { motion } from 'framer-motion'
import { Mail } from 'lucide-react'
import { WHATSAPP_NUMBER } from '@/lib/products'

export function Newsletter() {
  return (
    <section className="w-full py-20 bg-gradient-to-br from-primary to-secondary text-white">
      <div className="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        {/* Header }
        <motion.div
          initial={{ opacity: 0, y: -20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
        >
          <div className="flex justify-center mb-4">
            <Mail className="w-12 h-12 text-white" />
          </div>
          
          <h2 className="text-4xl md:text-5xl font-black mb-4">
            Ofertas Exclusivas
          </h2>
          
          <p className="text-lg text-white/90 mb-8">
            Seguinos en Instagram @Loqueresdrip Y Obtenes un 10% de descuento en tu compra. Mandanos la captura de que nos seguiste al WhatsApp
          </p>
        </motion.div>

        {/* CTA }
        <motion.div
          initial={{ opacity: 0, scale: 0.8 }}
          whileInView={{ opacity: 1, scale: 1 }}
          viewport={{ once: true }}
          className="flex flex-col sm:flex-row gap-4 justify-center"
        >
          <a
            href={`https://wa.me/${WHATSAPP_NUMBER}?text=Hola! Quiero descuento especial`}
            target="_blank"
            rel="noopener noreferrer"
            className="px-8 py-4 bg-white text-primary font-bold rounded-sm hover:bg-gray-100 transition-colors duration-300 inline-flex items-center justify-center gap-2"
          >
            <Mail className="w-5 h-5" />
            Contactar por WhatsApp
          </a>
        </motion.div>

        {/* Info }
        <motion.p
          initial={{ opacity: 0 }}
          whileInView={{ opacity: 1 }}
          transition={{ delay: 0.2 }}
          viewport={{ once: true }}
          className="mt-8 text-sm text-white/70"
        >
          Responderemos dentro de 24 horas | Sin spam garantizado
        </motion.p>
      </div>
    </section>
  )
}*/}
